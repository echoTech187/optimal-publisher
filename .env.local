# This file is for local development environment variables.
# It should not be committed to version control (.gitignore should include .env.local).

# The base URL for your backend API.
# NEXT_PUBLIC_ prefix is required to expose this variable to the browser/client-side.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
